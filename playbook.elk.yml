---
- hosts: all
  user: vagrant
  become: yes
  become_method: sudo
  roles:
    - { role: elasticsearch, tags: 'elasticsearch_setup' }
    - { role: kibana, tags: 'kibana_setup' }
    - logstash
    - { role: nginx, tags: 'nginx_setup' }
